
public class EnemyProjectile extends Projectile {
	private double radius = 2.0;					// raio (tamanho do projétil do inimigo
	
	public EnemyProjectile (double x, double y, double vx, double vy) {
		super (x, y, vx, vy);
	}
}